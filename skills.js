function calculateNumbers(var1, var2)   
{
    // This function takes two numbers and returns their sum
    return var1 + var2;
}
function calculateNumbersWithDefault(var1 = 0, var2 = 0)   
{
    // This function takes two numbers with default values and returns their sum
    return var1 + var2;
}